{% extends 'base.html.twig' %}

{% block title %}Diameter{% endblock %}

{% block body %}
    <h1>Diameter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ diameter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ diameter.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ diameter.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('diameter_index') }}">back to list</a>

    <a href="{{ path('diameter_edit', {'id': diameter.id}) }}">edit</a>

    {{ include('diameter/_delete_form.html.twig') }}
{% endblock %}