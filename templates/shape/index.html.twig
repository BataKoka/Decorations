{% extends 'base.html.twig' %}

{% block title %}Shape index{% endblock %}

{% block body %}
    <h1>
        Shape index
        <a href="{{ path('shape_new') }}" class="btn btn-success">Create new</a>
    </h1>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Active?</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for shape in shapes %}
            <tr>
                <td>{{ shape.name }}</td>
                <td>{{ shape.isActive ? 'Yes' : 'No' }}</td>
                <td>
                    <div class="btn-group" role="group" aria-label="Basic example">
                        {#<a href="{{ path('shape_show', {'id': shape.id}) }}">show</a>#}
                        <a href="{{ path('shape_edit', {'id': shape.id}) }}" class="btn btn-primary">Edit</a>
                        {{ include('shape/_delete_form.html.twig') }}
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}