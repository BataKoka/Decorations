{% extends 'base.html.twig' %}

{% block title %}Edit Location Type{% endblock %}

{% block body %}
    <h1>
        Edit Location Type: {{ location_type.name }}
        <a href="{{ path('location_type_index') }}" class="btn btn-outline-info">back to list</a>
    </h1>

    {{ include('location_type/_form.html.twig', {'button_label': 'Update'}) }}

    {#{{ include('location_type/_delete_form.html.twig') }}#}
{% endblock %}