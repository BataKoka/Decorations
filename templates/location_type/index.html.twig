{% extends 'base.html.twig' %}

{% block title %}Location Type index{% endblock %}

{% block body %}
    <h1>
        Location Type index
        <a href="{{ path('location_type_new') }}" class="btn btn-success">Create new</a>
    </h1>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Active?</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for location_type in location_types %}
            <tr>
                <td>{{ location_type.name }}</td>
                <td>{{ location_type.isActive ? 'Yes' : 'No' }}</td>
                <td>
                    <div class="btn-group" role="group" aria-label="Basic example">
                        {#<a href="{{ path('location_type_show', {'id': location_type.id}) }}">show</a>#}
                        <a href="{{ path('location_type_edit', {'id': location_type.id}) }}" class="btn btn-primary">Edit</a>
                        {{ include('location_type/_delete_form.html.twig') }}
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}