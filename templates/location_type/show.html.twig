{% extends 'base.html.twig' %}

{% block title %}LocationType{% endblock %}

{% block body %}
    <h1>LocationType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ location_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ location_type.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ location_type.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('location_type_index') }}">back to list</a>

    <a href="{{ path('location_type_edit', {'id': location_type.id}) }}">edit</a>

    {{ include('location_type/_delete_form.html.twig') }}
{% endblock %}