{% extends 'base.html.twig' %}

{% block title %}Celebration Type index{% endblock %}

{% block body %}
    <div class="form-group row">
        <div class="col-sm-7">
            <h1 class="h3 mb-3 font-weight-normal">Celebration Type index</h1>
        </div>
        <div class="col-sm-1">
            <a href="{{ path('celebration_type_new') }}" class="btn btn-success">Create new</a>
        </div>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Active?</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for celebration_type in celebration_types %}
            <tr>
                <td>{{ celebration_type.name }}</td>
                <td>{{ celebration_type.isActive ? 'Yes' : 'No' }}</td>
                <td>
                    <div class="btn-group" role="group" aria-label="Basic example">
                        {#<a href="{{ path('celebration_type_show', {'id': celebration_type.id}) }}">show</a>#}
                        <a href="{{ path('celebration_type_edit', {'id': celebration_type.id}) }}" class="btn btn-primary">Edit</a>
                        {{ include('celebration_type/_delete_form.html.twig') }}
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}