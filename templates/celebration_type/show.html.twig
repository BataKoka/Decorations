{% extends 'base.html.twig' %}

{% block title %}CelebrationType{% endblock %}

{% block body %}
    <h1>CelebrationType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ celebration_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ celebration_type.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ celebration_type.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('celebration_type_index') }}">back to list</a>

    <a href="{{ path('celebration_type_edit', {'id': celebration_type.id}) }}">edit</a>

    {{ include('celebration_type/_delete_form.html.twig') }}
{% endblock %}