{% extends 'base.html.twig' %}

{% block title %}Print Type index{% endblock %}

{% block body %}
    <div class="form-group row">
        <div class="col-sm-7">
            <h1 class="h3 mb-3 font-weight-normal">Print Type index</h1>
        </div>
        <div class="col-sm-1">
            <a href="{{ path('print_type_new') }}" class="btn btn-success">Create new</a>
        </div>
    </div>

    <table id="data_tables" class="table table-hover">
        <thead class="thead-light">
            <tr>
                <th>Active?</th>
                <th>Name</th>
                <th class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for print_type in print_types %}
            <tr>
                <td>{{ print_type.isActive ? '<span class="badge badge-success">Yes</span>' : '<span class="badge badge-danger">No</span>' }}</td>
                <td>{{ print_type.name }}</td>
                <td class="text-center">
                    <div class="btn-group btn-group-sm" role="group" aria-label="Basic example">
                        {#<a href="{{ path('print_type_show', {'id': print_type.id}) }}">show</a>#}
                        <a href="{{ path('print_type_edit', {'id': print_type.id}) }}" class="btn btn-primary">Edit</a>
                        {{ include('print_type/_delete_form.html.twig') }}
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}