{% extends 'base.html.twig' %}

{% block title %}Edit Print Type{% endblock %}

{% block body %}
    <h1>
        Edit Print Type: {{ print_type.name }}
        <a href="{{ path('print_type_index') }}" class="btn btn-outline-info">back to list</a>
    </h1>

    {{ include('print_type/_form.html.twig', {'button_label': 'Update'}) }}

    {#{{ include('print_type/_delete_form.html.twig') }}#}
{% endblock %}