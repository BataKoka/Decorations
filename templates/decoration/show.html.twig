{% extends 'base.html.twig' %}

{% block title %}Decoration{% endblock %}

{% block body %}
    <h1>Decoration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ decoration.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ decoration.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ decoration.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('decoration_index') }}">back to list</a>

    <a href="{{ path('decoration_edit', {'id': decoration.id}) }}">edit</a>

    {{ include('decoration/_delete_form.html.twig') }}
{% endblock %}