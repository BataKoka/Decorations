{% extends 'base.html.twig' %}

{% block title %}Balloon{% endblock %}

{% block body %}
    <h1>Balloon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ balloon.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ balloon.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ balloon.isActive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ balloon.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('balloon_index') }}">back to list</a>

    <a href="{{ path('balloon_edit', {'id': balloon.id}) }}">edit</a>

    {{ include('balloon/_delete_form.html.twig') }}
{% endblock %}