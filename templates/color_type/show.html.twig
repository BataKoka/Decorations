{% extends 'base.html.twig' %}

{% block title %}ColorType{% endblock %}

{% block body %}
    <h1>ColorType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ color_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ color_type.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ color_type.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('color_type_index') }}">back to list</a>

    <a href="{{ path('color_type_edit', {'id': color_type.id}) }}">edit</a>

    {{ include('color_type/_delete_form.html.twig') }}
{% endblock %}