{% extends 'base.html.twig' %}

{% block title %}Color Type index{% endblock %}

{% block body %}
    <div class="form-group row">
        <div class="col-sm-7">
            <h1 class="h3 mb-3 font-weight-normal">Color Type index</h1>
        </div>
        <div class="col-sm-1">
            <a href="{{ path('color_type_new') }}" class="btn btn-success">Create new</a>
        </div>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Active?</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for color_type in color_types %}
            <tr>
                <td>{{ color_type.name }}</td>
                <td>{{ color_type.isActive ? 'Yes' : 'No' }}</td>
                <td>
                    <div class="btn-group" role="group" aria-label="Basic example">
                        {#<a href="{{ path('color_type_show', {'id': color_type.id}) }}">show</a>#}
                        <a href="{{ path('color_type_edit', {'id': color_type.id}) }}" class="btn btn-primary">Edit</a>
                        {{ include('color_type/_delete_form.html.twig') }}
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}