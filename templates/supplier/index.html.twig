{% extends 'base.html.twig' %}

{% block title %}Supplier index{% endblock %}

{% block body %}
    <div class="form-group row">
        <div class="col-sm-7">
            <h1 class="h3 mb-3 font-weight-normal">Supplier index</h1>
        </div>
        <div class="col-sm-1">
            <a href="{{ path('supplier_new') }}" class="btn btn-success">Create new</a>
        </div>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Active?</th>
                <th>Name</th>
                <th>Country</th>
                <th class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for supplier in suppliers %}
            <tr>
                <td>{{ supplier.isActive ? '<span class="badge badge-success">Yes</span>' : '<span class="badge badge-danger">No</span>' }}</td>
                <td>{{ supplier.name }}</td>
                <td>{{ supplier.country.name }}</td>
                <td class="text-center">
                    <div class="btn-group" role="group" aria-label="Basic example">
                        {#<a href="{{ path('supplier_show', {'id': supplier.id}) }}">show</a>#}
                        <a href="{{ path('supplier_edit', {'id': supplier.id}) }}" class="btn btn-primary">Edit</a>
                        {{ include('supplier/_delete_form.html.twig') }}
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}