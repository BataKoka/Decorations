{% extends 'base.html.twig' %}

{% block title %}Edit Color{% endblock %}

{% block body %}
    <h1>
        Edit Color: {{ color.name }}
        <a href="{{ path('color_index') }}" class="btn btn-outline-info">back to list</a>
    </h1>

    {{ include('color/_form.html.twig', {'button_label': 'Update'}) }}

    {#{{ include('color/_delete_form.html.twig') }}#}
{% endblock %}